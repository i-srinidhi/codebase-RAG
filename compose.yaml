services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file: backend/.env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - codeatlas-data:/app/data
    restart: unless-stopped
volumes:
  codeatlas-data:
